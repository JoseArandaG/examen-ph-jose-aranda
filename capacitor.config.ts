import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'examen.ph.jose.aranda',
  appName: 'examen-ph-jose-aranda',
  webDir: 'www'
};

export default config;
